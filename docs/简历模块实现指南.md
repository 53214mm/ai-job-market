# 简历模块实现指南

> 项目: AI 智能招聘市场 | 模块: 简历模块 | 技术栈: Spring Boot 3.5.2 + MyBatis-Plus 3.5.9 + Spring AI + iText 9

---

## 目录

1. [整体流程概览](#1-整体流程概览)
2. [第 1 步：Entity 实体类](#2-第-1-步entity-实体类-7-个)
3. [第 2 步：DTO 和 VO](#3-第-2-步dto-和-vo-6-个)
4. [第 3 步：Mapper](#4-第-3-步mapper-7-个)
5. [第 4 步：Service](#5-第-4-步service-3-个)
6. [第 5 步：Controller](#6-第-5-步controller-1-个)
7. [第 6 步：AI 结构化输出 Record](#7-第-6-步ai-结构化输出-record)
8. [完整架构图](#8-完整架构图)
9. [测试验证](#9-测试验证)
10. [创建文件清单速查](#10-创建文件清单速查)
11. [前端页面设计](#11-前端页面设计)
12. [附录：关键设计决策](#12-附录关键设计决策)

---

## 1. 整体流程概览

### 1.1 简历 CRUD 流程

```
创建简历: POST /api/resumes → Controller → Service
  → 校验用户角色(必须是 SEEKER)
  → 保存 resume 主表（雪花ID）
  → 返回简历 ID

查看详情: GET /api/resumes/{id} → Service
  → 查 resume 主表
  → 分别查 education/work/project/skill/certificate 子表（按 sort_order 排序）
  → 组装 ResumeVO 返回

编辑简历: PUT /api/resumes/{id} → Service
  → 校验本人操作 + 简历存在
  → 更新基本信息字段（非子项）
```

### 1.2 子项管理流程（以教育经历为例）

```
添加: POST /api/resumes/{id}/education  → 校验本人 → insert 子表
更新: PUT  /api/resumes/{id}/education/{eid} → 校验归属 → update 子表
删除: DELETE /api/resumes/{id}/education/{eid} → 校验归属 → delete 子表
```

工作经历、项目经历、技能、证书的模式完全一致。

### 1.3 AI 分析流程

```
POST /api/resumes/{id}/ai-analyze → Service
  → 查简历主表 + 全部子项
  → 拼装成 Markdown 简历文本
  → 调用 JobApp.doChatWithReport(markdown, chatId)
  → Spring AI 将 LLM 返回 JSON 反序列化为 AiResumeAnalysis Record
  → 保存分析结果到 ai_resume_analysis 表
  → 更新 resume.ai_score 和 resume.ai_suggestion
  → 返回 AiResumeAnalysisVO
```

### 1.4 AI 优化流程

```
POST /api/resumes/{id}/ai-optimize → Service
  → 查简历 + 子项 → 拼装文本
  → 调用 JobApp.doChat(优化prompt, chatId)
  → LLM 返回优化后的内容（JSON 格式：每个字段的新值和修改说明）
  → 返回优化建议（不自动覆盖原简历，由用户确认后手动应用）
```

### 1.5 PDF 导出流程

```
POST /api/resumes/{id}/export-pdf → Service
  → 查简历 + 全部子项
  → 用 StringBuilder 拼装 HTML 格式的简历内容
  → 调用 PdfUtil.createPdf(path, content)
  → 返回文件下载 URL 或直接返回文件流
```

### 1.6 关键设计决策

| 决策点 | 方案 | 原因 |
|--------|------|------|
| 子项存储 | 独立子表 + sort_order 排序 | 灵活增删改，支持任意数量 |
| 子项 API | 嵌套路由 `/resumes/{id}/education` | RESTful，归属清晰 |
| AI 分析 | 复用 `JobApp.doChatWithReport()` | 已有结构化输出能力，无需新代码 |
| AI 优化 | 建议不回写 | 用户确认后再应用，安全性更高 |
| PDF 导出 | 复用 `PdfUtil.createPdf()` | 已有中文 PDF 生成能力 |
| 隐私控制 | 三级: PUBLIC / APPLICATION_ONLY / PRIVATE | 灵活控制简历可见性 |
| 默认简历 | 互斥设置，设新自动取消旧 | 投递时快速选择 |
| 角色限制 | 仅 SEEKER 可创建简历 | 与用户模块设计一致 |

---

## 2. 第 1 步：Entity 实体类 (7 个)

### 2.1 `Resume.java` — 简历主表

**路径：** `src/main/java/com/li/ai_job_market/model/entity/Resume.java`

```java
package com.li.ai_job_market.model.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;

import java.io.Serializable;
import java.time.LocalDateTime;

@Data
@TableName("resume")
public class Resume implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;

    private Long userId;              // 所属用户
    private String title;             // 简历名称
    private String fullName;
    private String email;
    private String phone;
    private String currentCity;
    private String expectedCity;
    private Integer expectedSalaryMin;
    private Integer expectedSalaryMax;
    private String jobStatus;         // 在职/离职/应届
    private String summary;           // 自我评价
    private String privacy;           // PUBLIC / APPLICATION_ONLY / PRIVATE
    private Boolean isDefault;        // 默认简历
    private Integer aiScore;          // AI 评分 0-100
    private String aiSuggestion;      // AI 优化建议

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updatedAt;

    @TableField(exist = false)
    private static final long serialVersionUID = 1L;
}
```

### 2.2 `ResumeEducation.java` — 教育经历

**路径：** `src/main/java/com/li/ai_job_market/model/entity/ResumeEducation.java`

```java
package com.li.ai_job_market.model.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;

import java.io.Serializable;
import java.time.LocalDate;
import java.time.LocalDateTime;

@Data
@TableName("resume_education")
public class ResumeEducation implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;

    private Long resumeId;
    private String schoolName;
    private String degree;            // 高中/大专/本科/硕士/博士
    private String major;
    private LocalDate startDate;
    private LocalDate endDate;
    private String description;
    private Integer sortOrder;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;

    @TableField(exist = false)
    private static final long serialVersionUID = 1L;
}
```

### 2.3 `ResumeWorkExperience.java` — 工作经历

**路径：** `src/main/java/com/li/ai_job_market/model/entity/ResumeWorkExperience.java`

```java
package com.li.ai_job_market.model.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;

import java.io.Serializable;
import java.time.LocalDate;
import java.time.LocalDateTime;

@Data
@TableName("resume_work_experience")
public class ResumeWorkExperience implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;

    private Long resumeId;
    private String companyName;
    private String position;
    private String industry;
    private LocalDate startDate;
    private LocalDate endDate;
    private String description;
    private String achievement;
    private String skillsUsed;        // 使用的技能(逗号分隔)
    private Integer sortOrder;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;

    @TableField(exist = false)
    private static final long serialVersionUID = 1L;
}
```

### 2.4 `ResumeProject.java` — 项目经历

```java
@Data
@TableName("resume_project")
public class ResumeProject implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;
    private Long resumeId;
    private String projectName;
    private String role;
    private LocalDate startDate;
    private LocalDate endDate;
    private String description;
    private String technologies;
    private String achievement;
    private String projectUrl;
    private Integer sortOrder;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;
}
```

### 2.5 `ResumeSkill.java` — 技能标签

```java
@Data
@TableName("resume_skill")
public class ResumeSkill implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;
    private Long resumeId;
    private String skillName;
    private String proficiency;       // 初学/熟练/精通/专家
    private Integer monthsOfUse;
    private Integer sortOrder;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;
}
```

### 2.6 `ResumeCertificate.java` — 证书

```java
@Data
@TableName("resume_certificate")
public class ResumeCertificate implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;
    private Long resumeId;
    private String certName;
    private String issuingOrg;
    private LocalDate obtainDate;
    private Integer sortOrder;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;
}
```

### 2.7 `AiResumeAnalysis.java` — AI 分析记录

```java
@Data
@TableName("ai_resume_analysis")
public class AiResumeAnalysis implements Serializable {

    @TableId(type = IdType.ASSIGN_ID)
    private Long id;
    private Long resumeId;
    private Long seekerId;
    private Integer overallScore;
    private Integer formatScore;
    private Integer contentScore;
    private Integer keywordScore;
    private String strengths;
    private String weaknesses;
    private String suggestions;
    private String optimizedContent;   // JSON

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;
}
```

---

## 3. 第 2 步：DTO 和 VO (6 个)

### 3.1 `ResumeCreateRequest.java`

**路径：** `src/main/java/com/li/ai_job_market/model/dto/resume/ResumeCreateRequest.java`

```java
package com.li.ai_job_market.model.dto.resume;

import lombok.Data;
import java.io.Serializable;

@Data
public class ResumeCreateRequest implements Serializable {
    private String title;
    private String fullName;
    private String email;
    private String phone;
    private String currentCity;
    private String expectedCity;
    private Integer expectedSalaryMin;
    private Integer expectedSalaryMax;
    private String jobStatus;
    private String summary;
    private String privacy;           // 默认 PUBLIC
    private static final long serialVersionUID = 1L;
}
```

### 3.2 `ResumeUpdateRequest.java`

```java
package com.li.ai_job_market.model.dto.resume;

import lombok.Data;
import java.io.Serializable;

@Data
public class ResumeUpdateRequest implements Serializable {
    private Long id;
    private String title;
    private String fullName;
    private String email;
    private String phone;
    private String currentCity;
    private String expectedCity;
    private Integer expectedSalaryMin;
    private Integer expectedSalaryMax;
    private String jobStatus;
    private String summary;
    private String privacy;
    private static final long serialVersionUID = 1L;
}
```

### 3.3 `ResumeQueryRequest.java`

```java
package com.li.ai_job_market.model.dto.resume;

import com.li.ai_job_market.common.PageRequest;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.io.Serializable;

@Data
@EqualsAndHashCode(callSuper = true)
public class ResumeQueryRequest extends PageRequest implements Serializable {
    private Long userId;
    private String title;
    private String privacy;
    private static final long serialVersionUID = 1L;
}
```

### 3.4 `SubItemRequest.java` — 子项通用请求

```java
package com.li.ai_job_market.model.dto.resume;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDate;

@Data
public class SubItemRequest implements Serializable {
    // 通用
    private Long id;                    // 更新时传入
    private Integer sortOrder;

    // 教育
    private String schoolName;
    private String degree;
    private String major;
    private LocalDate startDate;
    private LocalDate endDate;
    private String description;

    // 工作
    private String companyName;
    private String position;
    private String industry;
    private String achievement;
    private String skillsUsed;

    // 项目
    private String projectName;
    private String role;
    private String technologies;
    private String projectUrl;

    // 技能
    private String skillName;
    private String proficiency;         // 初学/熟练/精通/专家
    private Integer monthsOfUse;

    // 证书
    private String certName;
    private String issuingOrg;
    private LocalDate obtainDate;

    private static final long serialVersionUID = 1L;
}
```

### 3.5 `ResumeVO.java` — 简历详情 VO

```java
package com.li.ai_job_market.model.vo;

import com.li.ai_job_market.model.entity.*;
import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;
import java.util.List;

@Data
public class ResumeVO implements Serializable {
    // 主表字段
    private Long id;
    private Long userId;
    private String title;
    private String fullName;
    private String email;
    private String phone;
    private String currentCity;
    private String expectedCity;
    private Integer expectedSalaryMin;
    private Integer expectedSalaryMax;
    private String jobStatus;
    private String summary;
    private String privacy;
    private Boolean isDefault;
    private Integer aiScore;
    private String aiSuggestion;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;

    // 子项列表（按 sort_order 排序）
    private List<ResumeEducation> educations;
    private List<ResumeWorkExperience> workExperiences;
    private List<ResumeProject> projects;
    private List<ResumeSkill> skills;
    private List<ResumeCertificate> certificates;

    private static final long serialVersionUID = 1L;
}
```

### 3.6 `ResumeAnalysisVO.java` — AI 分析结果 VO

```java
package com.li.ai_job_market.model.vo;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

@Data
public class ResumeAnalysisVO implements Serializable {
    private Long id;
    private Long resumeId;
    private Integer overallScore;
    private Integer formatScore;
    private Integer contentScore;
    private Integer keywordScore;
    private String strengths;
    private String weaknesses;
    private String suggestions;
    private String optimizedContent;    // JSON 格式
    private LocalDateTime createdAt;
    private static final long serialVersionUID = 1L;
}
```

---

## 4. 第 3 步：Mapper (7 个)

全部继承 `BaseMapper`，零额外代码：

```java
package com.li.ai_job_market.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.li.ai_job_market.model.entity.Resume;

public interface ResumeMapper extends BaseMapper<Resume> {
}
```

其他六个同理：`ResumeEducationMapper`、`ResumeWorkExperienceMapper`、`ResumeProjectMapper`、`ResumeSkillMapper`、`ResumeCertificateMapper`、`AiResumeAnalysisMapper`。

---

## 5. 第 4 步：Service (3 个)

### 5.1 `ResumeService.java` — 接口

**路径：** `src/main/java/com/li/ai_job_market/service/ResumeService.java`

```java
package com.li.ai_job_market.service;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.IService;
import com.li.ai_job_market.model.dto.resume.ResumeCreateRequest;
import com.li.ai_job_market.model.dto.resume.ResumeQueryRequest;
import com.li.ai_job_market.model.dto.resume.ResumeUpdateRequest;
import com.li.ai_job_market.model.dto.resume.SubItemRequest;
import com.li.ai_job_market.model.entity.Resume;
import com.li.ai_job_market.model.entity.*;
import com.li.ai_job_market.model.vo.ResumeAnalysisVO;
import com.li.ai_job_market.model.vo.ResumeVO;
import org.springframework.core.io.Resource;

import java.util.List;

public interface ResumeService extends IService<Resume> {

    // === 简历 CRUD ===
    long createResume(Long userId, ResumeCreateRequest req);
    ResumeVO getResumeDetail(Long resumeId, Long currentUserId);
    boolean updateResume(Long resumeId, Long userId, ResumeUpdateRequest req);
    boolean deleteResume(Long resumeId, Long userId);
    Page<Resume> listMyResumes(Long userId, ResumeQueryRequest req);
    boolean setDefault(Long resumeId, Long userId);

    // === 子项 CRUD ===
    ResumeEducation addEducation(Long resumeId, Long userId, SubItemRequest req);
    ResumeEducation updateEducation(Long resumeId, Long eid, Long userId, SubItemRequest req);
    boolean deleteEducation(Long resumeId, Long eid, Long userId);

    ResumeWorkExperience addWorkExperience(Long resumeId, Long userId, SubItemRequest req);
    ResumeWorkExperience updateWorkExperience(Long resumeId, Long wid, Long userId, SubItemRequest req);
    boolean deleteWorkExperience(Long resumeId, Long wid, Long userId);

    ResumeProject addProject(Long resumeId, Long userId, SubItemRequest req);
    ResumeProject updateProject(Long resumeId, Long pid, Long userId, SubItemRequest req);
    boolean deleteProject(Long resumeId, Long pid, Long userId);

    ResumeSkill addSkill(Long resumeId, Long userId, SubItemRequest req);
    ResumeSkill updateSkill(Long resumeId, Long sid, Long userId, SubItemRequest req);
    boolean deleteSkill(Long resumeId, Long sid, Long userId);

    ResumeCertificate addCertificate(Long resumeId, Long userId, SubItemRequest req);
    ResumeCertificate updateCertificate(Long resumeId, Long cid, Long userId, SubItemRequest req);
    boolean deleteCertificate(Long resumeId, Long cid, Long userId);

    // === AI 功能 ===
    ResumeAnalysisVO aiAnalyze(Long resumeId, Long userId);
    String aiOptimize(Long resumeId, Long userId);

    // === PDF ===
    Resource exportPdf(Long resumeId, Long userId);

    // === 工具 ===
    ResumeVO toResumeVO(Resume resume);
    String buildResumeMarkdown(Long resumeId);
    void checkResumeAccess(Long resumeId, Long userId);
}
```

### 5.2 `ResumeServiceImpl.java` — 核心实现（关键方法）

**路径：** `src/main/java/com/li/ai_job_market/service/impl/ResumeServiceImpl.java`

```java
package com.li.ai_job_market.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.li.ai_job_market.AI.app.JobApp;
import com.li.ai_job_market.AI.tools.PdfUtil;
import com.li.ai_job_market.constant.FileConstant;
import com.li.ai_job_market.exception.ErrorCode;
import com.li.ai_job_market.exception.ThrowUtils;
import com.li.ai_job_market.mapper.*;
import com.li.ai_job_market.model.dto.resume.*;
import com.li.ai_job_market.model.entity.*;
import com.li.ai_job_market.model.vo.*;
import com.li.ai_job_market.service.ResumeService;
import jakarta.annotation.Resource;
import lombok.extern.slf4j.Slf4j;
import org.apache.commons.lang3.StringUtils;
import org.springframework.core.io.FileSystemResource;
import org.springframework.core.io.Resource;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.io.File;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;
import java.util.List;

@Slf4j
@Service
public class ResumeServiceImpl extends ServiceImpl<ResumeMapper, Resume>
        implements ResumeService {

    @Resource
    private JobApp jobApp;

    @Resource
    private ResumeEducationMapper educationMapper;
    @Resource
    private ResumeWorkExperienceMapper workMapper;
    @Resource
    private ResumeProjectMapper projectMapper;
    @Resource
    private ResumeSkillMapper skillMapper;
    @Resource
    private ResumeCertificateMapper certMapper;
    @Resource
    private AiResumeAnalysisMapper analysisMapper;

    private static final DateTimeFormatter DATE_FMT = DateTimeFormatter.ofPattern("yyyy.MM");

    // ==================== 简历 CRUD ====================

    @Override
    public long createResume(Long userId, ResumeCreateRequest req) {
        ThrowUtils.throwIf(StringUtils.isBlank(req.getTitle()),
                ErrorCode.PARAMS_ERROR, "简历标题不能为空");
        ThrowUtils.throwIf(StringUtils.isBlank(req.getFullName()),
                ErrorCode.PARAMS_ERROR, "姓名不能为空");

        Resume resume = new Resume();
        resume.setUserId(userId);
        resume.setTitle(req.getTitle());
        resume.setFullName(req.getFullName());
        resume.setEmail(req.getEmail());
        resume.setPhone(req.getPhone());
        resume.setCurrentCity(req.getCurrentCity());
        resume.setExpectedCity(req.getExpectedCity());
        resume.setExpectedSalaryMin(req.getExpectedSalaryMin());
        resume.setExpectedSalaryMax(req.getExpectedSalaryMax());
        resume.setJobStatus(req.getJobStatus());
        resume.setSummary(req.getSummary());
        resume.setPrivacy(StringUtils.isBlank(req.getPrivacy()) ? "PUBLIC" : req.getPrivacy());
        resume.setIsDefault(false);

        boolean saved = this.save(resume);
        ThrowUtils.throwIf(!saved, ErrorCode.SYSTEM_ERROR, "创建简历失败");
        log.info("简历创建: id={}, userId={}", resume.getId(), userId);
        return resume.getId();
    }

    @Override
    public ResumeVO getResumeDetail(Long resumeId, Long currentUserId) {
        Resume resume = this.getById(resumeId);
        ThrowUtils.throwIf(resume == null, ErrorCode.NOT_FOUND_ERROR, "简历不存在");

        // 隐私检查：PRIVATE 简历仅本人可见；APPLICATION_ONLY 不在此展示
        if ("PRIVATE".equals(resume.getPrivacy())) {
            checkResumeAccess(resumeId, currentUserId);
        }

        ResumeVO vo = toResumeVO(resume);

        // 加载子项
        vo.setEducations(listEducations(resumeId));
        vo.setWorkExperiences(listWorkExperiences(resumeId));
        vo.setProjects(listProjects(resumeId));
        vo.setSkills(listSkills(resumeId));
        vo.setCertificates(listCertificates(resumeId));

        return vo;
    }

    @Override
    public boolean updateResume(Long resumeId, Long userId, ResumeUpdateRequest req) {
        checkResumeAccess(resumeId, userId);

        Resume resume = new Resume();
        resume.setId(resumeId);
        if (req.getTitle() != null) resume.setTitle(req.getTitle());
        if (req.getFullName() != null) resume.setFullName(req.getFullName());
        if (req.getEmail() != null) resume.setEmail(req.getEmail());
        if (req.getPhone() != null) resume.setPhone(req.getPhone());
        if (req.getCurrentCity() != null) resume.setCurrentCity(req.getCurrentCity());
        if (req.getExpectedCity() != null) resume.setExpectedCity(req.getExpectedCity());
        if (req.getExpectedSalaryMin() != null) resume.setExpectedSalaryMin(req.getExpectedSalaryMin());
        if (req.getExpectedSalaryMax() != null) resume.setExpectedSalaryMax(req.getExpectedSalaryMax());
        if (req.getJobStatus() != null) resume.setJobStatus(req.getJobStatus());
        if (req.getSummary() != null) resume.setSummary(req.getSummary());
        if (req.getPrivacy() != null) resume.setPrivacy(req.getPrivacy());

        return this.updateById(resume);
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public boolean deleteResume(Long resumeId, Long userId) {
        checkResumeAccess(resumeId, userId);

        // 级联删除子项
        deleteByResumeId(educationMapper, resumeId);
        deleteByResumeId(workMapper, resumeId);
        deleteByResumeId(projectMapper, resumeId);
        deleteByResumeId(skillMapper, resumeId);
        deleteByResumeId(certMapper, resumeId);

        return this.removeById(resumeId);
    }

    // ==================== 子项 CRUD（以 Education 为例）====================

    @Override
    public ResumeEducation addEducation(Long resumeId, Long userId, SubItemRequest req) {
        checkResumeAccess(resumeId, userId);
        ThrowUtils.throwIf(StringUtils.isBlank(req.getSchoolName()),
                ErrorCode.PARAMS_ERROR, "学校名称不能为空");

        ResumeEducation edu = new ResumeEducation();
        edu.setResumeId(resumeId);
        edu.setSchoolName(req.getSchoolName());
        edu.setDegree(req.getDegree());
        edu.setMajor(req.getMajor());
        edu.setStartDate(req.getStartDate());
        edu.setEndDate(req.getEndDate());
        edu.setDescription(req.getDescription());
        edu.setSortOrder(req.getSortOrder() != null ? req.getSortOrder() : 0);

        educationMapper.insert(edu);
        return edu;
    }

    // updateEducation / deleteEducation 同理（字段拷贝 + 校验归属）

    // Work/Project/Skill/Certificate 的子项方法完全一致，仅实体类型不同

    // ==================== AI 分析 ====================

    @Override
    public ResumeAnalysisVO aiAnalyze(Long resumeId, Long userId) {
        checkResumeAccess(resumeId, userId);

        // 1. 拼装简历文本
        String resumeMarkdown = buildResumeMarkdown(resumeId);

        // 2. 构造分析 prompt + 调用 AI
        String prompt = """
            你是一位资深 HR 和简历优化专家。请分析以下简历，给出详细评分和建议。
            
            请以 JSON 格式返回，包含以下字段：
            - overallScore: 综合评分 0-100
            - formatScore: 格式评分 0-100
            - contentScore: 内容评分 0-100
            - keywordScore: 关键词匹配评分 0-100
            - strengths: 优势分析（String）
            - weaknesses: 不足之处（String）
            - suggestions: 优化建议（String，具体可操作）
            
            简历内容：
            %s
            """.formatted(resumeMarkdown);

        String result = jobApp.doChat(prompt, "resume-analyze-" + resumeId);

        // 3. 解析 AI 返回的 JSON（简化示例：假设 AI 返回纯 JSON）
        //    生产环境建议用 doChatWithReport() 配合 Record 做到结构化输出
        //    这里简化为从响应中提取 JSON 并解析

        // 4. 保存分析结果
        AiResumeAnalysis analysis = new AiResumeAnalysis();
        analysis.setResumeId(resumeId);
        analysis.setSeekerId(userId);
        // analysis.setOverallScore(...);  // 从 AI 响应中解析
        analysisMapper.insert(analysis);

        // 5. 更新 resume 的 aiScore
        // Resume resume = new Resume();
        // resume.setId(resumeId);
        // resume.setAiScore(analysis.getOverallScore());
        // resume.setAiSuggestion(analysis.getSuggestions());
        // this.updateById(resume);

        // 返回 VO
        ResumeAnalysisVO vo = new ResumeAnalysisVO();
        vo.setResumeId(resumeId);
        // ...字段拷贝
        return vo;
    }

    // ==================== AI 优化 ====================

    @Override
    public String aiOptimize(Long resumeId, Long userId) {
        checkResumeAccess(resumeId, userId);
        String resumeMarkdown = buildResumeMarkdown(resumeId);

        String prompt = """
            你是一位顶级简历优化顾问。请根据以下简历内容，提供优化建议。
            
            对每个字段给出「原文」和「优化后」的对比，以及简短说明。
            以 JSON 格式返回：{"suggestions": [{"field": "...", "original": "...", "optimized": "...", "reason": "..."}]}
            
            简历内容：
            %s
            """.formatted(resumeMarkdown);

        return jobApp.doChat(prompt, "resume-optimize-" + resumeId);
    }

    // ==================== PDF 导出 ====================

    @Override
    public Resource exportPdf(Long resumeId, Long userId) {
        checkResumeAccess(resumeId, userId);
        ResumeVO vo = getResumeDetail(resumeId, userId);

        StringBuilder sb = new StringBuilder();
        sb.append("========== 个人简历 ==========\n\n");
        sb.append("姓名: ").append(vo.getFullName()).append("\n");
        sb.append("电话: ").append(vo.getPhone()).append("\n");
        sb.append("邮箱: ").append(vo.getEmail()).append("\n");
        sb.append("期望城市: ").append(vo.getExpectedCity()).append("\n");
        sb.append("期望薪资: ")
          .append(vo.getExpectedSalaryMin()).append("K - ")
          .append(vo.getExpectedSalaryMax()).append("K\n\n");

        sb.append("---------- 自我评价 ----------\n");
        sb.append(vo.getSummary()).append("\n\n");

        sb.append("---------- 教育经历 ----------\n");
        for (ResumeEducation edu : vo.getEducations()) {
            sb.append(edu.getSchoolName()).append(" | ")
              .append(edu.getDegree()).append(" | ")
              .append(edu.getMajor()).append(" | ")
              .append(formatDate(edu.getStartDate())).append(" - ")
              .append(formatDate(edu.getEndDate())).append("\n");
            sb.append("  ").append(edu.getDescription()).append("\n\n");
        }

        // ... 工作经历、项目经历、技能同理

        String fileName = "resume_" + resumeId + "_" + System.currentTimeMillis() + ".pdf";
        String filePath = FileConstant.FILE_SAVE_DIR + File.separator + fileName;
        PdfUtil.createPdf(filePath, sb.toString());

        return new FileSystemResource(filePath);
    }

    // ==================== 辅助方法 ====================

    @Override
    public ResumeVO toResumeVO(Resume resume) {
        ResumeVO vo = new ResumeVO();
        vo.setId(resume.getId());
        vo.setUserId(resume.getUserId());
        vo.setTitle(resume.getTitle());
        vo.setFullName(resume.getFullName());
        vo.setEmail(resume.getEmail());
        vo.setPhone(resume.getPhone());
        vo.setCurrentCity(resume.getCurrentCity());
        vo.setExpectedCity(resume.getExpectedCity());
        vo.setExpectedSalaryMin(resume.getExpectedSalaryMin());
        vo.setExpectedSalaryMax(resume.getExpectedSalaryMax());
        vo.setJobStatus(resume.getJobStatus());
        vo.setSummary(resume.getSummary());
        vo.setPrivacy(resume.getPrivacy());
        vo.setIsDefault(resume.getIsDefault());
        vo.setAiScore(resume.getAiScore());
        vo.setAiSuggestion(resume.getAiSuggestion());
        vo.setCreatedAt(resume.getCreatedAt());
        vo.setUpdatedAt(resume.getUpdatedAt());
        return vo;
    }

    @Override
    public String buildResumeMarkdown(Long resumeId) {
        ResumeVO vo = getResumeDetail(resumeId, null);  // 内部调用不过权限
        StringBuilder sb = new StringBuilder();
        sb.append("# 个人简历\n\n");
        sb.append("**姓名:** ").append(vo.getFullName()).append("\n");
        sb.append("**城市:** ").append(vo.getCurrentCity())
          .append(" | **期望:** ").append(vo.getExpectedCity()).append("\n");
        sb.append("**薪资:** ").append(vo.getExpectedSalaryMin()).append("K-")
          .append(vo.getExpectedSalaryMax()).append("K\n\n");
        sb.append("## 自我评价\n").append(vo.getSummary()).append("\n\n");

        sb.append("## 教育经历\n");
        for (ResumeEducation e : vo.getEducations()) {
            sb.append("- ").append(e.getSchoolName()).append(" | ")
              .append(e.getDegree()).append(" | ").append(e.getMajor()).append("\n");
        }

        sb.append("\n## 工作经历\n");
        for (ResumeWorkExperience w : vo.getWorkExperiences()) {
            sb.append("- **").append(w.getCompanyName()).append("** - ")
              .append(w.getPosition()).append("\n");
            sb.append("  ").append(w.getDescription()).append("\n");
        }

        // ... 其他子项同理
        return sb.toString();
    }

    @Override
    public void checkResumeAccess(Long resumeId, Long userId) {
        Resume resume = this.getById(resumeId);
        ThrowUtils.throwIf(resume == null, ErrorCode.NOT_FOUND_ERROR, "简历不存在");
        ThrowUtils.throwIf(!resume.getUserId().equals(userId),
                ErrorCode.NO_AUTH_ERROR, "无权操作此简历");
    }

    // ==================== 内部辅助 ====================

    private List<ResumeEducation> listEducations(Long resumeId) {
        LambdaQueryWrapper<ResumeEducation> w = new LambdaQueryWrapper<>();
        w.eq(ResumeEducation::getResumeId, resumeId)
         .orderByAsc(ResumeEducation::getSortOrder);
        return educationMapper.selectList(w);
    }

    // listWorkExperiences / listProjects / listSkills / listCertificates 同理

    private <T> void deleteByResumeId(com.baomidou.mybatisplus.core.mapper.BaseMapper<T> mapper,
                                       Long resumeId) {
        // 通过 mapper.delete(wrapper) 按 resume_id 删除
    }

    private String formatDate(LocalDate date) {
        return date != null ? date.format(DATE_FMT) : "至今";
    }
}
```

> **注意：** 完整代码中各子项的 CRUD 方法模式完全一致（仅实体类型不同），文档中为节省篇幅仅展示 Education。另外 AI 分析中的结构化输出建议使用 Spring AI 的 `call().entity(Record.class)` 方式（`JobApp.doChatWithReport()` 已支持），避免手动解析 JSON。

### 5.3 `ResumeEducationService.java` — 子项 Service（可选）

如果需要在 Controller 中直接注入 Service 而非通过 ResumeService 统一调用，可以为每个子项创建独立的 Service。但推荐方案是全部通过 `ResumeService` 统一管理，保持一致性和事务控制。

```java
// 仅在需要独立 Service 时创建
@Service
public class ResumeEducationServiceImpl
        extends ServiceImpl<ResumeEducationMapper, ResumeEducation>
        implements ResumeEducationService {
}
```

---

## 6. 第 5 步：Controller (1 个)

### 6.1 `ResumeController.java`

**路径：** `src/main/java/com/li/ai_job_market/controller/ResumeController.java`

```java
package com.li.ai_job_market.controller;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.li.ai_job_market.common.BaseResponse;
import com.li.ai_job_market.common.ResultUtils;
import com.li.ai_job_market.exception.ErrorCode;
import com.li.ai_job_market.exception.ThrowUtils;
import com.li.ai_job_market.model.dto.resume.*;
import com.li.ai_job_market.model.entity.*;
import com.li.ai_job_market.model.enums.UserRoleEnum;
import com.li.ai_job_market.model.vo.*;
import com.li.ai_job_market.service.ResumeService;
import com.li.ai_job_market.service.UserService;
import jakarta.annotation.Resource;
import jakarta.servlet.http.HttpServletRequest;
import lombok.extern.slf4j.Slf4j;
import org.springframework.core.io.Resource;
import org.springframework.http.HttpHeaders;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@Slf4j
@RestController
@RequestMapping("/resumes")
public class ResumeController {

    @Resource
    private ResumeService resumeService;

    @Resource
    private UserService userService;

    private Long getLoginUserId(HttpServletRequest request) {
        UserVO user = userService.getLoginUser(request);
        ThrowUtils.throwIf(!UserRoleEnum.SEEKER.getValue().equals(user.getRole()),
                ErrorCode.NO_AUTH_ERROR, "仅求职者可操作简历");
        return user.getId();
    }

    // ==================== 简历 CRUD ====================

    @PostMapping
    public BaseResponse<Long> create(@RequestBody ResumeCreateRequest req,
                                      HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        long resumeId = resumeService.createResume(userId, req);
        return ResultUtils.success(resumeId);
    }

    @GetMapping
    public BaseResponse<Page<Resume>> list(ResumeQueryRequest req,
                                            HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        Page<Resume> page = resumeService.listMyResumes(userId, req);
        return ResultUtils.success(page);
    }

    @GetMapping("/{id}")
    public BaseResponse<ResumeVO> detail(@PathVariable Long id,
                                          HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        ResumeVO vo = resumeService.getResumeDetail(id, userId);
        return ResultUtils.success(vo);
    }

    @PutMapping("/{id}")
    public BaseResponse<Boolean> update(@PathVariable Long id,
                                         @RequestBody ResumeUpdateRequest req,
                                         HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        boolean ok = resumeService.updateResume(id, userId, req);
        return ResultUtils.success(ok);
    }

    @DeleteMapping("/{id}")
    public BaseResponse<Boolean> delete(@PathVariable Long id,
                                         HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        boolean ok = resumeService.deleteResume(id, userId);
        return ResultUtils.success(ok);
    }

    @PutMapping("/{id}/default")
    public BaseResponse<Boolean> setDefault(@PathVariable Long id,
                                             HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        boolean ok = resumeService.setDefault(id, userId);
        return ResultUtils.success(ok);
    }

    // ==================== 教育经历 ====================

    @PostMapping("/{id}/education")
    public BaseResponse<ResumeEducation> addEducation(
            @PathVariable Long id, @RequestBody SubItemRequest req,
            HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        ResumeEducation edu = resumeService.addEducation(id, userId, req);
        return ResultUtils.success(edu);
    }

    @PutMapping("/{id}/education/{eid}")
    public BaseResponse<ResumeEducation> updateEducation(
            @PathVariable Long id, @PathVariable Long eid,
            @RequestBody SubItemRequest req, HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        ResumeEducation edu = resumeService.updateEducation(id, eid, userId, req);
        return ResultUtils.success(edu);
    }

    @DeleteMapping("/{id}/education/{eid}")
    public BaseResponse<Boolean> deleteEducation(
            @PathVariable Long id, @PathVariable Long eid,
            HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        boolean ok = resumeService.deleteEducation(id, eid, userId);
        return ResultUtils.success(ok);
    }

    // 工作经历 POST/PUT/DELETE /{id}/work/{wid}   — 同理
    // 项目经历 POST/PUT/DELETE /{id}/project/{pid} — 同理
    // 技能     POST/PUT/DELETE /{id}/skill/{sid}   — 同理
    // 证书     POST/PUT/DELETE /{id}/cert/{cid}    — 同理

    // ==================== AI 功能 ====================

    @PostMapping("/{id}/ai-analyze")
    public BaseResponse<ResumeAnalysisVO> aiAnalyze(
            @PathVariable Long id, HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        ResumeAnalysisVO result = resumeService.aiAnalyze(id, userId);
        return ResultUtils.success(result);
    }

    @PostMapping("/{id}/ai-optimize")
    public BaseResponse<String> aiOptimize(
            @PathVariable Long id, HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        String result = resumeService.aiOptimize(id, userId);
        return ResultUtils.success(result);
    }

    @PostMapping("/{id}/export-pdf")
    public ResponseEntity<Resource> exportPdf(
            @PathVariable Long id, HttpServletRequest request) {
        Long userId = getLoginUserId(request);
        Resource resource = resumeService.exportPdf(id, userId);

        return ResponseEntity.ok()
                .contentType(MediaType.APPLICATION_PDF)
                .header(HttpHeaders.CONTENT_DISPOSITION,
                        "attachment; filename=\"resume.pdf\"")
                .body(resource);
    }
}
```

**API 路由总览（13 个端点 + 5 组子项 CRUD）：**

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/resumes` | 创建简历 | SEEKER |
| GET | `/api/resumes` | 我的简历列表(分页) | SEEKER |
| GET | `/api/resumes/{id}` | 简历详情(含全部子项) | SEEKER(本人/公开) |
| PUT | `/api/resumes/{id}` | 更新基本信息 | SEEKER(本人) |
| DELETE | `/api/resumes/{id}` | 删除简历+子项 | SEEKER(本人) |
| PUT | `/api/resumes/{id}/default` | 设为默认 | SEEKER(本人) |
| POST/PUT/DELETE | `/api/resumes/{id}/education[/{eid}]` | 教育经历 CRUD | SEEKER(本人) |
| POST/PUT/DELETE | `/api/resumes/{id}/work[/{wid}]` | 工作经历 CRUD | SEEKER(本人) |
| POST/PUT/DELETE | `/api/resumes/{id}/project[/{pid}]` | 项目经历 CRUD | SEEKER(本人) |
| POST/PUT/DELETE | `/api/resumes/{id}/skill[/{sid}]` | 技能 CRUD | SEEKER(本人) |
| POST/PUT/DELETE | `/api/resumes/{id}/cert[/{cid}]` | 证书 CRUD | SEEKER(本人) |
| POST | `/api/resumes/{id}/ai-analyze` | AI 分析简历 | SEEKER(本人) |
| POST | `/api/resumes/{id}/ai-optimize` | AI 优化简历 | SEEKER(本人) |
| POST | `/api/resumes/{id}/export-pdf` | 导出 PDF 简历 | SEEKER(本人) |

---

## 7. 第 6 步：AI 结构化输出 Record

为了利用 `JobApp.doChatWithReport()` 实现类型安全的结构化输出，定义一个 Java Record：

**路径：** `src/main/java/com/li/ai_job_market/model/record/ResumeAnalysisRecord.java`

```java
package com.li.ai_job_market.model.record;

public record ResumeAnalysisRecord(
    int overallScore,
    int formatScore,
    int contentScore,
    int keywordScore,
    String strengths,
    String weaknesses,
    String suggestions
) {}
```

在 `ResumeServiceImpl.aiAnalyze()` 中使用：

```java
ResumeAnalysisRecord analysis = jobApp.doChatWithReport(prompt, "resume-analyze-" + resumeId,
    ResumeAnalysisRecord.class);
```

`JobApp` 内部使用 Spring AI 的 `call().entity(ResumeAnalysisRecord.class)` 自动将 LLM 返回的 JSON 反序列化为 `ResumeAnalysisRecord`。

---

## 8. 完整架构图

```
┌──────────────────────────────────────────────────────────┐
│                    前端 (Vue 3)                           │
│  /resumes 列表 → /resumes/{id} 详情 → /resumes/{id}/edit │
│  /resumes/{id}/analysis AI 报告                           │
└──────────────────────┬───────────────────────────────────┘
                       │ REST API
┌──────────────────────▼───────────────────────────────────┐
│                ResumeController.java                      │
│  @RestController @RequestMapping("/resumes")             │
│  简历 CRUD + 5 组子项 CRUD + AI + PDF                     │
│  角色限制: getLoginUserId() → 校验 SEEKER 角色            │
└──────────────────────┬───────────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────────┐
│              ResumeServiceImpl.java                       │
│  createResume() / getResumeDetail() / updateResume()     │
│  addEducation() / addWorkExperience() / ...              │
│  aiAnalyze() → buildResumeMarkdown() → JobApp.doChat()   │
│  aiOptimize() → buildResumeMarkdown() → JobApp.doChat()  │
│  exportPdf() → buildResumeMarkdown() → PdfUtil.createPdf│
└──────┬─────────────────────┬─────────────────────────────┘
       │                     │
┌──────▼──────┐    ┌─────────▼──────────┐
│  8 个 Mapper │    │   AI 复用          │
│  (Resume +   │    │  JobApp (对话)     │
│   6 子表 +   │    │  PdfUtil (PDF)     │
│   analysis)  │    │  Redis 聊天记忆    │
└──────────────┘    └────────────────────┘
```

---

## 9. 测试验证

### 9.1 编译验证

```bash
cd G:\IDEA_NEW\product\ai_job_market
./mvnw clean compile
```

期望：BUILD SUCCESS

### 9.2 Swagger UI 测试

启动应用后，访问 `http://localhost:8123/api/swagger-ui/index.html`

#### 测试序列：

**1. 登录获取 Token**
```
POST /api/user/login
Body: {"email":"test2@test.com","password":"123456"}
→ 复制返回的 token
→ 在 Authorize 中粘贴 token
```

**2. 创建简历**
```
POST /api/resumes
Body: {
  "title": "我的第一份简历",
  "fullName": "张三",
  "email": "zhang@example.com",
  "phone": "13800138000",
  "currentCity": "北京",
  "expectedCity": "上海",
  "expectedSalaryMin": 20,
  "expectedSalaryMax": 35,
  "jobStatus": "在职",
  "summary": "5年Java开发经验，擅长Spring全家桶...",
  "privacy": "PUBLIC"
}
→ 返回 {"code":0, "data": <简历ID>}
```

**3. 添加教育经历**
```
POST /api/resumes/{id}/education
Body: {
  "schoolName": "清华大学",
  "degree": "本科",
  "major": "计算机科学",
  "startDate": "2015-09-01",
  "endDate": "2019-06-30",
  "description": "GPA 3.8，获得国家奖学金",
  "sortOrder": 0
}
```

**4. 查看简历详情**
```
GET /api/resumes/{id}
→ 返回包含教育经历、工作经历等子项的完整 ResumeVO
```

**5. AI 分析**
```
POST /api/resumes/{id}/ai-analyze
→ 返回 AiResumeAnalysisVO（评分 + 建议）
```

**6. PDF 导出**
```
POST /api/resumes/{id}/export-pdf
→ 下载 PDF 文件
```

### 9.3 数据库验证

```sql
-- 检查简历主表
SELECT id, user_id, title, full_name, privacy, ai_score FROM resume;

-- 检查子表数据完整性
SELECT resume_id, COUNT(*) FROM resume_education GROUP BY resume_id;
SELECT resume_id, COUNT(*) FROM resume_work_experience GROUP BY resume_id;
SELECT resume_id, COUNT(*) FROM resume_project GROUP BY resume_id;
SELECT resume_id, COUNT(*) FROM resume_skill GROUP BY resume_id;

-- 检查 AI 分析结果
SELECT * FROM ai_resume_analysis WHERE resume_id = <你的简历ID>;
```

---

## 10. 创建文件清单速查

| # | 文件路径 | 说明 |
|---|---------|------|
| 1 | `model/entity/Resume.java` | 简历主表实体 |
| 2 | `model/entity/ResumeEducation.java` | 教育经历实体 |
| 3 | `model/entity/ResumeWorkExperience.java` | 工作经历实体 |
| 4 | `model/entity/ResumeProject.java` | 项目经历实体 |
| 5 | `model/entity/ResumeSkill.java` | 技能标签实体 |
| 6 | `model/entity/ResumeCertificate.java` | 证书实体 |
| 7 | `model/entity/AiResumeAnalysis.java` | AI 分析记录实体 |
| 8 | `model/dto/resume/ResumeCreateRequest.java` | 创建简历 DTO |
| 9 | `model/dto/resume/ResumeUpdateRequest.java` | 更新简历 DTO |
| 10 | `model/dto/resume/ResumeQueryRequest.java` | 查询简历 DTO |
| 11 | `model/dto/resume/SubItemRequest.java` | 子项通用请求 DTO |
| 12 | `model/vo/ResumeVO.java` | 简历详情 VO |
| 13 | `model/vo/ResumeAnalysisVO.java` | AI 分析结果 VO |
| 14 | `model/record/ResumeAnalysisRecord.java` | AI 结构化输出 Record |
| 15 | `mapper/ResumeMapper.java` | 简历 Mapper |
| 16 | `mapper/ResumeEducationMapper.java` | 教育经历 Mapper |
| 17 | `mapper/ResumeWorkExperienceMapper.java` | 工作经历 Mapper |
| 18 | `mapper/ResumeProjectMapper.java` | 项目经历 Mapper |
| 19 | `mapper/ResumeSkillMapper.java` | 技能 Mapper |
| 20 | `mapper/ResumeCertificateMapper.java` | 证书 Mapper |
| 21 | `mapper/AiResumeAnalysisMapper.java` | AI 分析 Mapper |
| 22 | `service/ResumeService.java` | 简历服务接口 |
| 23 | `service/impl/ResumeServiceImpl.java` | 简历服务实现 |
| 24 | `controller/ResumeController.java` | 简历控制器 |

**共 24 个 Java 文件，0 个修改现有文件。**

---

## 11. 前端页面设计

需在 `ai-job-market-frontend/src/pages/` 下新增：

| 路由 | 页面 | 核心组件 |
|------|------|---------|
| `/resumes` | 我的简历列表 | 简历卡片列表 + 创建按钮 + 设为默认 |
| `/resumes/create` | 创建简历 | 分步表单（基本信息 → 教育 → 工作 → 项目 → 技能） |
| `/resumes/{id}` | 简历详情 | 完整简历展示 + AI 评分 + 子项列表 |
| `/resumes/{id}/edit` | 编辑简历 | 各子项独立编辑面板 |
| `/resumes/{id}/analysis` | AI 分析报告 | 评分雷达图 + 优势/不足/建议卡片 |

依赖直接调用已创建的 API proxy（Vite 已配置 `/api → localhost:8123`）。

---

## 12. 附录：关键设计决策

### Q: 为什么子项不嵌入主 resume 表（用 JSON 列）？
**A:** 独立子表支持独立排序、独立增删改，MyBatis-Plus 直接 CRUD，比 JSON 操作更高效。同时子表可以独立建索引（如按技能名搜索）。

### Q: AI 分析和优化为什么不自动覆盖原简历？
**A:** AI 的建议不一定完全正确。用户应有权审核和选择性应用优化建议，避免 AI "幻觉"导致简历信息失真。

### Q: Privacy 和 isDefault 的关系？
**A:** `privacy` 控制谁可以看到（PUBLIC 所有人 / APPLICATION_ONLY 投递时 / PRIVATE 仅自己）。`isDefault` 控制投递时默认选哪份简历。两者互不影响。

### Q: 能否复用现有的 Service 模板？
**A:** 完全可以。`ResumeServiceImpl` 遵循与 `UserServiceImpl` 相同的模式（`extends ServiceImpl<Mapper, Entity>` + `implements Service`），异常处理、分页、响应格式全部一致。
